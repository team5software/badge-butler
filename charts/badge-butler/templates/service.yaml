apiVersion: v1
kind: Service
metadata:
  name: {{ include "badge-butler.fullname" . }}
  labels:
    {{- include "badge-butler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "badge-butler.selectorLabels" . | nindent 4 }}
